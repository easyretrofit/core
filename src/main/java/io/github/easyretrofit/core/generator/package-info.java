/**
 * The Retrofit.Builder() object in the native Retrofit , it is necessary to initialize the CallAdapterFactory, ConverterFactory, OkHttpClient, CallFactory, CallBackExecutor objects,<br>
 * In easy retrofit, these objects are configured through annotation and injected through CDI,<br>
 * this packages is responsible for generating these objects.
 */
package io.github.easyretrofit.core.generator;